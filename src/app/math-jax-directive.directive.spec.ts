import { MathJaxDirectiveDirective } from './math-jax-directive.directive';

describe('MathJaxDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MathJaxDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
